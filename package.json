{
  "name": "chat-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "test": "mocha -R spec",
    "postinstall": "cd client && npm install"
  },
  "dependencies": {
    "cluster": "^0.7.7",
    "express": "^4.15.5",
    "path": "^0.12.7",
    "redis": "^2.8.0",
    "socket.io": "^2.0.3",
    "socket.io-redis": "^5.2.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^4.0.1"
  }
}
